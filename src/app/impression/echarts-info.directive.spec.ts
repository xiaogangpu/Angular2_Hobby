import { EchartsInfoDirective } from './echarts-info.directive';

describe('EchartsInfoDirective', () => {
  it('should create an instance', () => {
    const directive = new EchartsInfoDirective();
    expect(directive).toBeTruthy();
  });
});
